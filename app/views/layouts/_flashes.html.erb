<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div data-alert class="alert-box <%= name %> radius">
      <%= content_tag :div, msg %>
      <a class="close">&times;</a>
    </div>
  <% end %>
<% end %>